"""Legacy main.py - Redirects to new modular structureThis file maintains backward compatibility while the new modular structure is in app/"""# Import the new modular appfrom app.main import app# Re-export for backward compatibility__all__ = ["app"]# Note: The application has been refactored to a modular structure.# New code should use the app/ directory structure.# This file is kept for backward compatibility.## To run the application:# 1. Use: uvicorn main:app --reload (legacy)# 2. Or: uvicorn app.main:app --reload (recommended)# 3. Or: python -m app.main (recommended)